

\begin{figure}[ht]
\centering
\begin{tikzpicture}[
  node distance=0.55cm and 2.2cm,
  box/.style={rectangle, rounded corners=3pt, draw=black!70, fill=blue!8,
              text width=2.6cm, align=center, font=\small, minimum height=0.65cm},
  reactbox/.style={rectangle, rounded corners=3pt, draw=black!70, fill=orange!10,
              text width=2.6cm, align=center, font=\small, minimum height=0.65cm},
  toolbox/.style={rectangle, rounded corners=3pt, draw=teal!70, fill=teal!8,
              text width=2.6cm, align=center, font=\small, minimum height=0.65cm},
  outbox/.style={rectangle, rounded corners=3pt, draw=green!60!black, fill=green!8,
              text width=2.6cm, align=center, font=\small, minimum height=0.65cm},
  arrow/.style={-{Stealth[length=5pt]}, thick},
]

%% ── LEFT SIDE: Fixed Pipeline ─────────────────────────────────────────────
\node[font=\bfseries\small, text width=2.6cm, align=center] (titleL)
      {Fixed Pipeline};

\node[box, below=0.4cm of titleL] (p1) {Prompt Generation};
\node[box, below=of p1]           (p2) {LLM Query};
\node[box, below=of p2]           (p3) {Entity Extraction};
\node[box, below=of p3]           (p4) {Web Research (scripted)};
\node[box, below=of p4]           (p5) {Merge \& Score};
\node[outbox, below=of p5]        (p6) {Output CSV};

\draw[arrow] (p1) -- (p2);
\draw[arrow] (p2) -- (p3);
\draw[arrow] (p3) -- (p4);
\draw[arrow] (p4) -- (p5);
\draw[arrow] (p5) -- (p6);

%% ── RIGHT SIDE: ReAct Loop ────────────────────────────────────────────────
\node[font=\bfseries\small, text width=3cm, align=center,
      right=5.6cm of titleL] (titleR) {ReAct Agent (Agent 2)};

\node[reactbox, below=0.4cm of titleR]  (r1) {\textbf{Thought} (reasoning)};
\node[reactbox, below=of r1]            (r2) {\textbf{Action} (tool call)};
\node[toolbox,  below=of r2]            (r3) {MCP Tools (Maps, TA, IG\ldots)};
\node[reactbox, below=of r3]            (r4) {\textbf{Observation} (tool result)};
\node[outbox,   below=of r4]            (r5) {JSON Output};

\draw[arrow] (r1) -- (r2);
\draw[arrow] (r2) -- (r3);
\draw[arrow] (r3) -- (r4);
\draw[arrow] (r4) -- (r5);

%% Loop-back arrow: Observation -> Thought (if more steps needed)
\draw[arrow, dashed, bend left=55]
  (r4.west) to node[left, font=\scriptsize, text width=1cm, align=center]
  {if more steps} (r1.west);

%% ── Vertical divider ─────────────────────────────────────────────────────
\coordinate (mid) at ($(titleL.east)!0.5!(titleR.west)$);
\draw[dashed, gray!50, thick] (mid |- titleL.north) ++(0,0.3)
  -- ++(0,-10.2);

\end{tikzpicture}
\caption{Fixed pipeline architecture (left) versus the ReAct agentic loop
         (right) used in Agent~2. The fixed pipeline executes a predetermined
         sequence; the ReAct loop lets the LLM reason, select tools
         dynamically, and iterate until sufficient evidence is gathered.}
\label{fig:pipeline_vs_react}
\end{figure}
